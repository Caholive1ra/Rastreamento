# Exemplo de variáveis de ambiente para Docker
# Copie este arquivo para .env e configure seus valores reais
# NUNCA commite o arquivo .env!

# Database
SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/database?sslmode=require
SPRING_DATASOURCE_USERNAME=seu_usuario
SPRING_DATASOURCE_PASSWORD=sua_senha_segura

# User passwords (BCrypt hashed)
# Para gerar um novo hash, use: https://bcrypt-generator.com/
APP_ADMIN_PASSWORD=$2a$10$SUA_HASH_AQUI
APP_CLIENT_PASSWORD=$2a$10$SUA_HASH_AQUI

# Postgres local (para docker-compose)
POSTGRES_PASSWORD=senha_postgres_local
